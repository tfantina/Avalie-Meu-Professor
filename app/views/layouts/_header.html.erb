<nav class="navbar navbar-default" role="navigation">
  <div class="conatiner">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
          <span class="sr-only">Toggle Navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to (image_tag("http://dev.travisfantina.com/services/AvaliarMeuProfessor/Logo.png", alt: "Avalie Meu Professor", class: "navbar-brand")), root_url %>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
          <li> <%= link_to "Todos os professores", professors_path %> </li>

          <li> <%= link_to "Novo professor", new_professor_path, class: "active" %> </li>


        <% if user_signed_in? %>
          <% if current_user.admin? %>
              <li><%= link_to "Admin Pannel", admin_url(current_user.id) %></li>
          <% end %>
            <li><%=  link_to "Log Out", destroy_user_session_path, method: "delete" %>
        <% end %>


      </ul>
      <% if !current_page?(root_url) %>
      <%= form_tag search_professors_path, method: :get, class: "navbar-form navbar-right", role: "Search" do %>
      <p>
        <%= text_field_tag :search, params[:search], class: "form-control" %>
        <%= submit_tag "Presquisar", name: nil, class: "btn btn-default" %>
      </p>
      <% end %>
      <% end %>


    </div>
</div>
</nav>
