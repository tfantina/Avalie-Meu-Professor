 <div class="intro">
    <h1> Professor Review </h1>
    <p> Read reviews before chooing a classes, rate your professors at the end of the semester
      and sign in to add a professor.  This will be translated to Portuguese shortly, launching in October! </p>
      <p> With over <%= @num %> professors we are your go to choice!</p>

      <p> Search for a professor, school, department or <%= link_to "click to see the full database.", professors_path %> </p>

        <%= form_tag search_professors_path, method: :get, class: "form form-sm", role: "Search" do %>
      <p>
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Professor" %>
        <%= submit_tag "Search", name: nil, class: "btn btn-success" %>
      </p>
      <% end %>
</div>

  <div class="row">
  <% @footer.each do |professor| %>
     <div class=".col-sm-6 col-md-3">
       <div class="thumbnail">

         <h3><%= professor.try(:fullname) %></h3>
         <ul class="list-unstyled text-muted">
           <li> <%= professor.try(:school) %></li>
           <li class="small"> <%= professor.try(:department) %></li>
         </ul>
       </div>
      </div>
    <% end %>

  </div>
