
<div class="jumbotron jumbotron-bg">
 <div class="intro">
    <p> Descrição: Pesquise antes de começar as suas aulas. Avalie (ou adicione) seu professor no final do semestre.
 </p>
    <!--  <p> With over <%= @num %> professors we are your go to choice!</p> -->

      <p> Pesquise seu professor ou <%= link_to "sua faculdade no banco de dados.", professors_path %> </p>
    <div class="search-bar">
        <%= form_tag search_professors_path, method: :get, class: "form form-sm", role: "Search" do %>
      <p>
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Professor" %>
        <%= submit_tag "Pesquisar", name: nil, class: "btn btn-primary" %>
      </p>
      <% end %>
    </div>
</div>

<div class="recent-profs">

<h1>Professores Recentemente Adicionado</h1>

  <div class="row">
  <% @footer.each do |professor| %>
     <div class=".col-sm-6 col-md-3">
       <%= link_to professor, class: "thumbnail" do %>

         <h3><%= professor.try(:fullname) %></h3>
         <ul class="list-unstyled text-muted">
           <li> <%= professor.try(:school) %></li>
           <li class="small"> <%= professor.try(:department) %></li>
         </ul>
         <% end %>
       </div>
    <% end %>
  </div>
</div>
  </div>
